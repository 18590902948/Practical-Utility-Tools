# 脚本设计通用规范

本文件记录本仓库所有 Python / Shell 脚本设计的通用约定。新增或修改脚本时，必须遵守以下规范。

> **样板脚本**：`testing_design/xyz2model_xyz.py` 是符合本规范全部条款的规范样板脚本，设计新脚本时可对照参考。

## 第一：设计参考 —— perfect_design

`perfect_design/` 目录下的脚本（作者 Zihan YAN 的 GPUMDkit 风格）设计完善，是本仓库脚本设计的**主要参考**，多数设计习惯由此借鉴：

1. **头部模板注释块**：每个脚本文件顶部必须使用 `=====` 分隔的模板注释块，依次写明（字段名称对齐、排版美观）：
   - 脚本名、分类
   - 功能：一句话准确全面简洁地概括功能
   - 使用方法：命令行用法
   - 参数：逐条讲解每个参数（如 `--continue/-con`），一行一个
   - 模式参数：脚本运行模式说明，一律用 `--全称/-缩写` 选项形式逐行列出，注明默认模式与各模式行为差异（见第九节）
   - 输入文件、输出文件（含记录文件，见第 5 条）
   - 输出路径：相对路径说明
   - 作者、最后修改日期
   - 必要时附 ASCII 目录树示例，直观说明脚本所在目录的结构

   模板示意：

   ```
   """
   =============================================================================
   脚本:        XXX.py
   分类:        （脚本分类）
   功能:        （一句话准确全面简洁概括功能）
   使用方法:    XXX.py -con 文件 参数
   参数:        --continue/-con .......
                （下一个参数讲解，一行一个）
   模式参数:    --extract/-ext   固定抽帧（默认）：提取指定帧号 [帧号 ...]
                --random/-ran   随机抽帧：随机抽取 n 帧（用法：--random <n>）
                --all/-all      全帧抽取：抽取全部帧
                （每个模式一行：--全称/-缩写 + 行为说明）
   输入文件:    A.xyz
   输出文件:
     XXX.txt    （记录文件，一般都要设计，每次设计新脚本时询问用户）
     （其他输出文件说明）
   输出路径:    （相对路径说明，脚本优先相对路径设计）
   作者:        Hongbo Sun
   最后修改日期: 2026-08-20
   =============================================================================
   """
   ```
2. **命名规范**：
   - 格式转换类：`<源格式>2<目标格式>.py`（如 `cif2pos.py`、`exyz2pos.py`）
   - 流程类脚本：按执行顺序加数字前缀（如 `1xyz2poscar_letter.py`、`2cp_cal_file_letter.py`）
   - 变体用后缀区分（如 `_letter` / `_number`）
3. **README 维护**：工具目录配有 README，用表格汇总各脚本用法（功能、参数、输出）。
4. **实现参考**：结构文件处理优先用 ASE（`ase.io.read/write`）；批处理优先用 `glob` + 目录遍历；脚本支持 `python xxx.py <args>` 直接命令行运行。
5. **记录文件约定**：输出文件一般都要设计**记录文件**（如 `XXX.txt`，记录本次处理的结果清单，方便审计与复现）；**每次设计新脚本时，先询问用户是否需要记录文件及如何设计**。记录文件一般采取**表格形式**：**必须有表头**（以 `#` 开头）、**最少两列**（一列标识 + 至少一列内容），每行一条记录、空格分隔各列（如 帧号/原子数/路径/事件/状态，参考 `testing_design/xyz2model_xyz.py`）；表头内容、列数与列名由用户决定。报告类 txt（如检测/收集报告）采用**分节明细**结构：标题分隔线、时间戳（`%Y-%m-%d %H:%M:%S`）、按阶段分节（如 `[内部重复]`、`[输出文件]`）、末尾汇总统计，结构化便于审计（参考 `check_dup_xyz.py` 的 check_dup.txt）。
6. **输出模式约定**：脚本输出内容**默认采用覆盖模式**（直接覆盖同名旧文件）；**设计脚本时，先询问用户输出设计为追加还是覆盖**（如批量收集场景：首次覆盖、后续 append 追加，参考 `workflow_AL/batch_scf/4scf_collect_numder.py`）。

## 第二：测试流程 —— AI_laboratory 沙盒

- 写好脚本后，**一律先复制（cp）到 `AI_laboratory/` 目录测试**，不得直接在业务目录中运行验证。
- 测试时可以产生对应的测试文件（如小样本输入、测试输出），但**测试成功后必须删除测试文件**，保持 `AI_laboratory/` 整洁（当前为空目录）。
- 测试通过后，再回到原目录正式使用。

## 第三：代码风格

1. **脚本五区结构**：每个脚本自上而下依次分为五个区域，用分隔线注释标明：
   - **参数配置区**（用户方便修改）：import 之后立即放置，集中所有关键参数
   - **环境准备区**（运行环境准备）：终端编码（UTF-8）、依赖检查（如 ASE 导入）等环境准备代码，位于参数配置区之后
   - **函数配置区**（工具函数定义）：工具函数与常量映射（如 FORMAT_MAP）集中定义
   - **脚本工作区**（脚本工作流）：main() 中实现主流程（读取输入 → 处理 → 输出）
   - **脚本运行区**（启动）：`if __name__ == "__main__": main()`
2. **参数配置区格式**：`testing_design/orthocell.py` 仅作为**格式参考**（等号对齐、井号注释对齐、`====` 分隔线），保持脚本美观；配置区内容按以下**标准模板**组织：

   ```python
# ============================== 参数配置区 =====================================
   INPUT_FILES  = ["a.xyz", "b.xyz"]     # 输入文件列表 (支持通配符，相对 INPUT_PATH 展开；命令行参数优先)
   OUTPUT_FILES = ["A.xyz", "B.xyz"]     # 输出文件列表 (支持通配符，相对 OUTPUT_PATH 展开；命令行参数优先)
   RECORD_FILE  = "xxx.txt"              # 记录文件 (脚本所在目录)
   INPUT_PATH   = "./"                   # 输入文件寻找路径 (相对脚本所在目录)
   OUTPUT_PATH  = "./"                   # 输出文件寻找路径 (相对脚本所在目录)
   # ===========================================================================
   ```

   **配置区一般包含以上 5 个参数**（输入文件、输出文件、记录文件、输入文件寻找路径、输出文件寻找路径）；其余关键参数（精度阈值、缓冲大小、记录文件表头等）按脚本功能追加，凡用户后期可能需要调整的值都集中于此，不照搬示例参数。

   **通配符**：输入/输出文件列表支持 glob 通配符（如 `["*.xyz"]` 表示寻找路径下所有 xyz 文件），文件很多时无需逐个列出；展开相对对应的寻找路径进行。

   **路径基准**：INPUT_PATH/OUTPUT_PATH 为相对路径时**相对于脚本所在目录**解析（默认 `"./"` 即脚本所在目录，从任意目录调用脚本结果稳定）；命令行参数相对当前运行目录解析（见第八节）。

   **命令行优先**：脚本通过命令行传参运行时，默认**忽略参数配置区中对应的参数**；配置区仅作为未通过命令行指定时的默认值（命令行参数 > 配置区默认值）。
3. **中文注释**：注释、提示信息一律使用中文。
4. **函数化**：功能拆分为独立函数，每个函数带一行 docstring 说明用途。
5. **Windows 编码处理**：脚本开头（环境准备区）处理 Windows 控制台 GBK 无法输出 emoji 的问题，统一切换 UTF-8：

   ```python
   if hasattr(sys.stdout, 'reconfigure'):
       sys.stdout.reconfigure(encoding='utf-8')
       sys.stderr.reconfigure(encoding='utf-8')
   ```

6. **输出风格**：终端输出用 emoji + 中文状态符号，直观可读：
   - `✅` 成功 / `❌` 错误 / `⚠️` 警告 / `ℹ️` 提示 / `📦` 处理中 / `🎉` 完成
   - 清单类信息用编号 + 右对齐表格展示
   - 表格含中文时按**显示宽度对齐**（中文等宽字符按 2 列计，用 `dw/pad/col` 辅助函数，参考 `workflow_AL/batch_scf/4scf_collect_numder.py`），避免中文列错位
   - 失败/跳过原因映射为**简短状态标签**（如 缺少OUTCAR/未收敛/自洽异常/读取异常），便于表格简洁展示（参考 `skip_status`）
7. **脚本自定位**：用 `SCRIPT_DIR = os.path.dirname(os.path.abspath(__file__))` 定位脚本所在目录，脚本即放即用，不依赖绝对路径。
8. **排版整洁**：代码等宽对齐（等号、井号、缩进统一），注释分隔线（`# ==== 区块 ====`）分区，区块之间空行分隔，保持排版整齐干净。
9. **功能清晰简洁**：脚本单一职责、流程清晰，函数短小精悍，避免冗余代码与重复逻辑。

## 第四：交互与防御性设计

1. **显式确认**：所有交互输入必须显式输入后回车（防止误触）；输入 `q` 可随时退出脚本。
2. **编号输入解析**：支持 `all`、`1,3,5`、`1-3` 等混合格式，非法输入提示后重新输入。
3. **执行前清单确认**：批量操作前先展示来源清单与目标清单（含数量统计，如 `N × M = N*M 次复制`），用户确认后才执行。
4. **缺失容错**：关键文件/目录缺失时，能自动生成的（如默认 INCAR、默认提交脚本模板）自动创建并提示；无法自动处理的报错退出并给出补救说明。
5. **数据校验**：涉及数据对应关系的操作必须校验（如 POTCAR 与 POSCAR 元素顺序一致性），不一致时明确报错。
6. **冲突处理**：检测同名冲突，提供覆盖/跳过选项。
7. **运行完毕总结关键信息**：脚本运行结束后必须集中总结关键信息，便于用户一眼掌握运行结果，包括：处理数量统计（成功/跳过/失败）、输出文件与路径（绝对路径）、关键结果值等。

## 第五：命令行运行规范

1. **命令行运行是脚本必有的运行方式**：每个脚本都必须支持 `python 脚本名 <参数>` 直接命令行运行。
2. **帮助能力**：每个脚本必须支持 `python 脚本名 -h`（或 `--help`），展示脚本的主要功能与使用方法（即头部注释块内容），并以退出码 0 结束。
3. **简写/全写选项**：选项同时提供简写与全写两种形式：
   - `-h` 是 `--help` 的简写：显示帮助
   - `-o` 是 `--output` 的简写（输出目录也可写作 `--outdir`）：指定输出路径；合并类脚本中支持**两种形式**：以 `.xyz/.extxyz` 结尾视为**输出文件完整路径**（如 `-o ./C/c.xyz`，扩展名决定输出格式），否则视为**输出目录**（如 `-o .`，文件名用配置区 OUTPUT_FILES）
4. **选项位置随意**：选项与位置参数可任意排列，不依赖先后顺序（如 `python xxx.py in.xyz 5 -o out` 与 `python xxx.py -o out in.xyz 5` 等价）。
5. **无参数容错**：缺少必要参数时打印使用说明并退出（或进入交互模式），不得静默崩溃。
6. 参数解析可用 `argparse`，也可手写 `parse_args` 循环解析（`arg in ("-o", "--outdir")` 模式，参考 `testing_design/xyz2model_xyz.py`）。
7. 用 `argparse` 时，自定义 Formatter 组合 `RawDescriptionHelpFormatter + ArgumentDefaultsHelpFormatter`（参考 `perfect_design/GPUMD_sample/sample_structures.py`），使 `-h` 保留原始帮助排版并自动显示各参数默认值。

## 第六：帧序数约定（遵循 OVITO 规则）

1. **0 起始索引**：命令行传入的帧号一律为 OVITO 中显示的 0 起始索引（0 = 第一帧）。
2. **命名一致**：输出文件夹/文件按 OVITO 帧号本身命名（如 `5/`、`9/`、`66/model.xyz`），与 OVITO 显示一致，方便对照。
3. **两种视角提示**：OVITO 帧 n 在文本编辑器中是第 n+1 帧（1 起始）；脚本头部注释必须写明帧号约定，运行时打印两种视角便于当场核对（参考 `testing_design/xyz2model_xyz.py`）。
4. 若脚本确需采用其他帧号约定（如 1 起始），必须在头部注释中显著声明并说明与 OVITO 帧号的换算关系，避免混淆。

## 第七：Linux 环境适配（超算）

1. 本仓库大多数脚本都在**超算（Linux）环境**运行，必须保证 Linux 兼容：
   - 路径拼接一律用 `os.path.join`，不得硬编码 `\` 或 `/`
   - 写入供超算使用的文件（INCAR、提交脚本等）时强制 LF 换行（`newline="\n"`）、UTF-8 无 BOM
   - 不依赖 Windows 特有命令、路径或编码（如 GBK）
   - `.sh` 脚本以 `#!/bin/bash` 开头、LF 换行
2. 终端输出统一 UTF-8（`sys.stdout.reconfigure(encoding="utf-8")`），避免中文 / emoji 输出报错。
3. 脚本应在无图形界面的终端环境中可运行（不依赖 GUI）。

## 第八：路径约定（相对优先）

1. 脚本优先按**相对路径**设计：默认读写位置为脚本所在目录 / 当前运行目录，脚本可整体拷贝到任意目录直接运行，不写死绝对路径。
2. 命令行参数同时支持**相对路径与绝对路径**，相对路径解析遵循**不带点默认脚本目录**规则：
   - **输出路径（-o）**：以 `./`、`../` 或 `.` 开头的相对路径相对**当前运行目录**解析（如 `-o ./D/E/f.xyz` 输出到当前运行目录下）；**不带点开头**的相对路径默认相对**脚本所在目录**解析（如 `-o D/E/f.xyz` 输出到脚本所在目录下的 D/E/f.xyz）；绝对路径照旧。
   - **输入文件/文件夹参数**：先按当前运行目录探测（bash 自动展开的通配符路径即相对当前运行目录），不存在再相对脚本所在目录解析。
3. 终端打印与记录文件中展示**绝对路径**，便于核对与复现。
4. 路径处理参考 `testing_design/xyz2model_xyz.py`：输入文件直接用文件名（相对路径）给出，输出根目录默认脚本所在目录，可用 `-o` 指定自定义路径。
5. **多输入跨路径合并与 -o 输出规则**：合并类脚本的输入文件可直接带相对/绝对路径，支持跨目录合并（如 `python 脚本名 ./A/a.xyz ./B/b.xyz -o ./C/c.xyz`）；`-o/--output` 支持**两种形式**：
   - `-o 输出文件`：以 `.xyz/.extxyz` 结尾视为**输出文件完整路径**（扩展名决定输出格式，仅合并同格式输入，其他格式跳过提示），如 `-o ./D/E/f.xyz`；
   - `-o 输出目录`：非 `.xyz/.extxyz` 结尾视为**输出目录**（如 `-o .` 输出到当前目录、`-o ./` 输出到当前目录），文件名用配置区 OUTPUT_FILES 默认名。
   **默认命名规则**：只要输出路径未写到文件名——省略 `-o` 或 `-o` 指定目录——输出文件一律用配置区 OUTPUT_FILES 默认名（如 `merged.xyz` / `merged.extxyz`）；省略 `-o` 时输出到默认输出目录（配置区 OUTPUT_PATH，如脚本目录下 `merge/`），`-o` 指定目录时输出到该目录。记录文件（如 `merged.txt`）与输出文件同目录。
   **示例统一带 `./`**：脚本头部示例、文档中的相对路径一律写成带 `./` 前缀的标准形式（如 `python xxx.py ./A/a.xyz -o ./C/c.xyz`），避免误导用户误解路径基准。
   **目标文件夹与靶文件夹**：合并类脚本中，格式一致、待合并的输入文件夹统称**目标文件夹**，输出路径所在的文件夹统称**靶文件夹**（docstring、记录文件统一使用这两个术语）。目标文件夹可用位置参数直接列出，也可用通配符描述（如 `'./1_md*'` 匹配当前运行目录下所有 `1_md` 开头的文件夹；glob 展开按文件夹名排序、仅保留目录）；文件夹合并类脚本（如 `xyzs2xyz_folder.py`）无参数时自动扫描脚本目录下所有子文件夹作为目标文件夹，并排除靶文件夹（输出目录），避免把输出当输入。
   **-t/--target 可选标记**：目标文件夹（或输入文件）位置参数前可加 `-t/--target` 标记（如 `python 脚本名 -t ./dir1 ./dir2 -s energy -o ./A/a.xyz`），仅为明确声明、可省略，位置参数一律视为目标文件夹（输入文件）；不传目标文件夹时使用配置区默认值（如 DEFAULT_DIRS）。
   **兼容不带 `./` 的写法**：脚本必须同时兼容两种写法——带 `./` 的规范写法（相对当前运行目录）与不带 `./` 的写法（相对脚本所在目录，见第 2 条"不带点默认脚本目录"规则），用户写哪种脚本都能理解、效果一致（如 `python 脚本名 A/a.xyz -o C/c.xyz`）；命令行通配符同样遵循此规则（不带点开头的模式在当前运行目录无匹配时，自动相对脚本所在目录再展开一次）。脚本头部示例与文档一律只展示带 `./` 的规范写法。
   **清洗合并类**（参考 `testing_design/dump_xyzs2xyz4clean.py`）：合并多目标文件夹下同名 dump/xyz 文件时（默认目标文件夹列表为配置区 DEFAULT_DIRS，条目支持通配符），注释行默认只保留结构标签（Time/pbc/Lattice/Properties），用 `-s/--save` 额外保留指定键值对（可多次，如 `-s energy -s virial`），用 `-n/--name` 指定输入文件名（默认 `dump.xyz`）。
6. **配置区与命令行路径基准不同**：参数配置区的 INPUT_PATH/OUTPUT_PATH 为相对路径时**相对于脚本所在目录**解析（默认 `"./"` 即脚本所在目录，从任意目录调用脚本结果稳定）；命令行参数路径解析规则见第 2 条（不带点开头的相对路径同样默认脚本所在目录，与配置区一致）。记录文件默认在输出目录（与输出文件同目录，见第 5 条）。

## 第九：模式参数设置规范

脚本有多个运行模式时，模式参数一律使用**选项形式**指定，**禁止**用位置参数形式（如 `XXX.py 文件 random <n>`）区分模式。

1. **选项形式**：每个模式对应一个选项，`--全称/-缩写` 成对提供（与 `-h/--help`、`-o/--output` 约定一致）：

   ```
   --extract/-ext   固定抽帧（默认）：提取指定帧号 [帧号 ...]
   --random/-ran   随机抽帧：随机抽取 n 帧（用法：--random <n>）
   --all/-all      全帧抽取：抽取全部帧
   ```

2. **命名规则**：全称用有意义的英文单词或单词组合（如 extract / random / all）；**缩写固定 3 个字母**：全称是单个单词时取前 3 个字母（extract → -ext、random → -ran）；全称是多单词组合（非单词）时取每个单词首字母凑够 3 个（全称一般设计为 3 个单词）；全称不足 3 个字母时缩写等于全称（如 --all/-all）；缩写不得与已有选项（如 -h、-o）冲突。
3. **默认模式**：不传模式选项时使用默认模式，默认模式在模板注释与帮助中标注（默认）。
4. **互斥校验**：模式选项互斥，同时指定多个时报错并提示用法。
5. **模式专属参数**：模式自带的参数跟在模式选项后（如 `--random 5`）。
6. **示例**：模板注释中每个模式给出一条完整命令行示例，便于直接复制使用。

## 第十：大数据量与性能设计

1. **缓冲区批量落盘**：处理海量结构时，内存缓冲攒够 N 个（如 50）再批量写入文件（首次覆盖、后续 append），写入后清空缓冲区释放内存，避免内存爆炸（参考 `workflow_AL/batch_scf/4scf_collect_numder.py`）。
2. **快速指纹 + 严格验证**：大规模重复/相似性判定时，先用快速指纹（原子数/元素组成/晶胞/排序量化后的分数坐标）粗分组，再在组内做严格验证（PBC 最小镜像距离一一对应），兼顾速度与准确性（参考 `testing_design/check_dup_xyz.py`）。

## 待补充

（后续有新约定，继续在此补充……）
